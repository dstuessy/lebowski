{
  "name": "lebowski",
  "version": "0.0.1",
  "description": "",
  "main": "src/lebowski/main.ts",
  "scripts": {
    "start": "tsc -p . --watch",
    "deploy-sandbox": "git subtree push --prefix sandbox origin gh-pages",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Daniel Stuessy",
  "license": "SEE LICENSE IN LICENSE",
  "devDependencies": {
    "typescript": "^2.9.2"
  }
}
